{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "electron",
			"request": "launch",
			"name": "Build Angular and debug Electron",
			"appDir": "${workspaceFolder}",
			"preLaunchTask": "build-angular"
		},
		{
			"type": "electron",
			"request": "launch",
			"name": "Debug Electron",
			"appDir": "${workspaceFolder}"
		}
	]
}