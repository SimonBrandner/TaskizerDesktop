import { OrdinalValidatorDirective } from "./ordinal-validator.directive";

describe("OrdinalDirective", () => {
	it("should create an instance", () => {
		const directive = new OrdinalValidatorDirective();
		expect(directive).toBeTruthy();
	});
});
